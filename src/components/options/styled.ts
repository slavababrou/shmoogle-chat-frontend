import styled from 'styled-components';

export const OptionsHeader = styled.label`
  color: ${({ theme }) => theme.text.secondaryColor};
  font-size: 0.9rem;
`;

export const OptionsBody = styled.label`
  margin-top: 20px;
`;
